package com.u.android_uhome.utils

import com.u.android_uhome.home.HomeModel

class Timer {

    fun storeTimer(timer: HomeModel.ResponseStartTimer) {
        val uid = timer.id
//        val service = EstimoteNotification(null)
//        service.startObserver(uid)
    }
}